import React from 'react';
import ServiceItem from './ServiceItem';

export default function ReactNativeService({ item, index }) {
  // Specific logic or styling for React Native can go here
  return <ServiceItem item={item} index={index} />;
}
